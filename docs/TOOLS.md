# Tool catalogue

Generated by `make tools-doc` from each tool's own header. Do not edit.

`porthole tools` is the searchable version of this; `porthole tools --json` is the machine-readable one.

**needs** is the device state a tool requires: `-` host-only, `BOOTED`, `FASTBOOT`, `on-device` (it runs *on* the device), or `any` (it probes and handles several).

164 tools.

## Boot, flash and recovery

| tool | needs | scope | what it does |
|---|---|---|---|
| `boot-probe.sh` | FASTBOOT | `soc:msm8998` | Boot an image and time the USB transitions, distinguishing a real reboot from noise. |
| `bootimg-cmdline.py` | - | `generic` | Read/patch the kernel cmdline of an Android boot image, in place or to a copy. |
| `bootimg-repack-dtb.py` | - | `generic` | Swap the appended DTB inside an Android boot image, fixing up the header. |
| `bootimg-verify.py` | - | `generic` | Verify a boot.img actually carries the kernel/DTB you just built. |
| `dtbo-split.py` | - | `generic` | Split an Android DTBO image into its individual .dtb entries. |
| `ph-flash-boot.sh` | FASTBOOT | `generic` | Flash a boot image to slot b and come back up in pmOS, end to end, unattended. |
| `ph-reboot.sh` | any | `generic` | Reboot the phone and return the INSTANT ssh answers again. |
| `ph-recover.sh` | any | `generic` | ph-recover.sh -- one command to get taimen back after the 2026-08-01 incident, |
| `ph-to-fastboot.sh` | any | `soc:qcom` | Get the phone INTO the bootloader, and return the instant it lands (~9s). |

## Device state and supervision

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-audio-cycle.sh` | BOOTED | `soc:msm8998` | ph-audio-cycle.sh [--dtb] [--kernel] -- one command from "make finished" to |
| `ph-cycle.sh` | any | `generic` | One display bring-up experiment: bootloader -> RAM boot -> modprobe -> capture. |
| `ph-deadman.sh` | BOOTED | `soc:msm8998` | ph-deadman.sh -- arm/disarm a self-reboot on the PHONE before a risky test. |
| `ph-device-test.sh` | BOOTED | `generic` | One check: two concurrent ph-device.sh callers must not interleave. |
| `ph-device.sh` | any | `generic` | Serialise access to the ONE physical phone across parallel agents. |
| `ph-hang-matrix.sh` | BOOTED | `soc:msm8998` | ph-hang-matrix.sh -- the 2x2 attribution matrix for defect #3. |
| `ph-lifeline` | on-device | `soc:qcom` | ph-lifeline: if this boot cannot reach the USB host, return to the bootloader. |
| `ph-soak.sh` | on-device | `generic` | ph-soak.sh -- the on-device half of the D2 / 72 h stability soak. |
| `ph-supervise.sh` | any | `generic` | ph-supervise.sh -- keep the phone alive during unattended work. |
| `ph-suspend-cycle.sh` | BOOTED | `soc:qcom` | One real s2idle cycle, driven from the host, with evidence that survives the |
| `ph-wake-cycle.py` | on-device | `generic` | Press the power key on a loop and count the blank/unblank transitions that |
| `ph-wifi-soak.sh` | on-device | `generic` | ph-wifi-soak.sh -- watch a WiFi link for the two ways it fails quietly. |
| `stallwatch.sh` | BOOTED | `generic` | stallwatch.sh -- detect the taimen PID-1 freeze from the HOST. |

## Power, suspend and thermal

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-afk.sh` | BOOTED | `generic` | ph-afk.sh -- stop the phone suspending itself while nobody is at the keyboard. |
| `ph-cpufreq-verify.sh` | - | `soc:msm8998` | ph-cpufreq-verify.sh -- does a requested CPU frequency actually get delivered? |
| `ph-osm-probe.py` | - | `soc:msm8998` | Read what the msm8998 OSM actually delivered. |
| `ph-suspend-guard-check.sh` | BOOTED | `generic` | ph-suspend-guard-check.sh -- prove the suspend guard actually blocks, WITHOUT |
| `ph-sysstate.sh` | on-device | `generic` | Print one compact line of system state per second, forever. |
| `ph-thermal-ramp.sh` | - | `generic` | ph-thermal-ramp.sh -- the six-phase thermal ramp, with an abort that works. |
| `ph-thermal.sh` | BOOTED | `generic` | Keep a phone from being cooked by back-to-back measurement arms. |
| `ph-usb-wake.py` | any | `soc:qcom` | Wake a taimen that is wedged in suspend, from the host, without hands. |

## Display and GPU

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-display-watch.py` | - | `soc:msm8998` | Log the display pipeline so a frozen screen can be read off afterwards. |
| `ph-dpu-regs.sh` | BOOTED | `generic` | Dump and decode the DPU registers that matter for a stuck display. |
| `ph-firstpaint.sh` | - | `generic` | ph-firstpaint.sh -- what the user actually waits for: launch -> first frame. |
| `ph-fps.py` | - | `soc:msm8998` | Sample the display/GPU pipeline once a second. Run ON THE DEVICE. |
| `ph-gesture-bench.py` | - | `generic` | Measure how smooth the phosh session actually is. Run ON THE DEVICE as root. |
| `ph-perf-ab.sh` | - | `generic` | ph-perf-ab.sh -- interleaved A/B of a tuning knob against real frame timings. |
| `ph-ringdump.py` | - | `generic` | Decode the ringbuffer out of an adreno devcoredump into CP packets. |

## Camera

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-allphy.py` | - | `soc:msm8998` | Hand-configure ALL THREE msm8998 CSIPHYs and watch every status register. |
| `ph-awb-invert.py` *(profile)* | - | `device:google-taimen` | Invert the SoftwareIsp pipeline over a crop to recover raw (black-subtracted, |
| `ph-camstream.sh` | - | `generic` | ph-camstream.sh -- one capture attempt on the IMX179 front camera, with the |
| `ph-chromatix-awb-locus.py` *(profile)* | - | `device:google-taimen` | Extract the AWB colour-temperature locus from a vendor chromatix 3a blob. |
| `ph-easel-iatu.py` *(profile)* | - | `device:google-taimen` | Program Easel's inbound iATU so BAR2 maps its peripheral window, then read |
| `ph-easel-poke.py` *(profile)* | - | `device:google-taimen` | Poke Easel registers live through BAR2 (HANDOFF §42b). |
| `ph-easel-poll.py` *(profile)* | - | `device:google-taimen` | Poll Easel RX PHY_STOPSTATE / PHY_RX fast, to catch HS data bursts. |
| `ph-easel-pon.py` *(profile)* | - | `device:google-taimen` | Power on Easel's BCM15602 PMIC by hand and see if it answers on i2c. |
| `ph-easel-power.py` *(profile)* | - | `device:google-taimen` | Bring Easel (Pixel Visual Core) all the way up from userspace, then retrain |
| `ph-easel-rx.py` *(profile)* | - | `device:google-taimen` | Read Easel's MIPI RX/TOP state through BAR2 -- a third, independent |
| `ph-easel-txpoll.py` *(profile)* | - | `device:google-taimen` | Poll Easel TX PHY_STATUS to see whether the bypass forwards data bursts.""" |
| `ph-easel-vpg.py` *(profile)* | - | `device:google-taimen` | Drive Easel's TX video pattern generator -- sensor and bypass mux out of the |
| `ph-phy-reinit.py` | - | `generic` | Re-run the CSIPHY v5.0.1 init sequence by hand, WHILE the sensor is already |
| `ph-phy-settle-sweep.py` | - | `generic` | Sweep the CSIPHY settle count over its whole 0..255 range against a LIVE |
| `ph-phy-sweep.py` | - | `generic` | Sweep CSIPHY CMN_CTRL5 (the lane-enable mask) against a LIVE transmitting |
| `ph-phystat.py` | - | `generic` | Read (and optionally poke) the SoC CSIPHY common block live. |
| `ph-sensorstream.sh` | - | `generic` | ph-sensorstream.sh -- stream the REAL sensor path, imx179 -> CSIPHY -> CSID -> |
| `ph-tgstream.sh` | - | `generic` | ph-tgstream.sh -- capture from the CSID's own test generator. |
| `ph-vfe-wrote.py` *(profile)* | - | `device:google-taimen` | Does the VFE write ANY byte to the capture buffer? |

## Audio

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-acoustic.py` | BOOTED | `generic` | ph-acoustic.py -- decide "does audio actually come out / go in" with no human. |
| `ph-alsa-stage.py` | BOOTED | `generic` | Walk the ALSA PCM lifecycle one call at a time, marking /dev/kmsg before each. |
| `ph-dmic-sweep.sh` *(profile)* | on-device | `device:google-taimen` | ph-dmic-sweep.sh [seconds] -- runs ON THE PHONE. |
| `ph-gap.py` | BOOTED | `generic` | Measure the capture gating (HANDOFF-audio.md 2.5) as a number, per config. |
| `ph-mic-ab.sh` *(profile)* | BOOTED | `device:google-taimen` | ph-mic-ab.sh [seconds] -- runs ON THE PHONE. |
| `ph-mic-check.sh` | BOOTED | `generic` | ph-mic-check.sh -- is the microphone producing AUDIO, or just a noise floor? |
| `ph-mic-clean.sh` *(profile)* | BOOTED | `device:google-taimen` | ph-mic-clean.sh [mic] [seconds] [stray] -- runs ON THE PHONE. |
| `ph-mic-split.sh` *(profile)* | BOOTED | `device:google-taimen` | ph-mic-split.sh -- runs ON THE PHONE. Three captures, one question each. |
| `ph-mic-test.sh` | BOOTED | `generic` | ph-mic-test.sh [DMIC0..DMIC5] [seconds] [rate] -- runs ON THE PHONE. |
| `ph-mic-watch.sh` | BOOTED | `generic` | ph-mic-watch.sh [seconds] -- runs ON THE PHONE. |
| `ph-micwatch.sh` | BOOTED | `generic` | ph-micwatch.sh -- sample whether anything is holding the microphone open. |
| `ph-tas-fw.py` *(profile)* | - | `device:google-taimen` | Parse a TAS2557 uCDSP firmware image and report what is inside it. |
| `ph-tone.py` | - | `generic` | Generate an acoustic probe signal, to be played on the HOST while the phone |
| `ph-wav-verdict.py` | - | `generic` | Decide whether a capture contains real microphone audio. |
| `ph-wlgaps.py` | - | `generic` | Where a client's late frames actually are, from a WAYLAND_DEBUG=1 log. |

## Touch and input

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-evtest.py` | - | `generic` | Dump multitouch events from an evdev node. Run ON THE DEVICE, as root. |
| `ph-ftm4-log.py` *(profile)* | - | `device:google-taimen` | Passively log the whole touch chain so a failure can be read off afterwards. |
| `ph-ftm4-poke.py` *(profile)* | - | `device:google-taimen` | Interrogate the FTM4 touch controller behind the back of a bound ftm4 driver. |
| `ph-key.py` | - | `generic` | Inject a key press through /dev/uinput. Run ON THE DEVICE as root. |
| `ph-mtstate.py` | - | `generic` | Print the CURRENT multitouch slot state of an evdev node, without waiting. |
| `ph-touch.py` | - | `generic` | Inject touch gestures through /dev/uinput. Run ON THE DEVICE as root. |

## Sensors

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-compass-cal.py` | BOOTED | `soc:qcom` | Calibrate the magnetometer's hard-iron offset, and persist it. |
| `ph-compass-check.py` | - | `generic` | Check the compass maths in qcom_smgr.c against floating point. |
| `ph-iio-events.py` | - | `soc:qcom` | Enable an IIO event and print events as they arrive. Runs ON the device. |
| `ph-iio-read.py` | - | `soc:qcom` | Read samples from a buffer-only IIO device (no in_*_raw), e.g. qcom-smgr-*. |
| `ph-mount-cal.py` | BOOTED | `generic` | Measure the accelerometer mount matrix from four guided poses. |
| `ph-prox-check.py` | BOOTED | `generic` | Check that qcom_smgr suppresses the proximity IR crosstalk in the ALS. |
| `ph-sensor-chain.sh` | BOOTED | `soc:qcom` | ph-sensor-chain.sh -- watch the WHOLE ambient-light / proximity chain at once: |
| `ph-sensor-poke.py` | - | `generic` | Live 16-bit-register I2C access to the IMX179, for use WHILE camss holds the |
| `ph-sns-groups.py` | - | `soc:qcom` | Extract the SSC sensor-registry group table from Android's sensors.qcom. |

## Radios

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-lte-load.py` | - | `generic` | Generate sustained traffic that REALLY goes out over LTE. Run ON THE DEVICE. |
| `ph-wifi-audit.sh` | - | `generic` | ph-wifi-audit.sh -- end-to-end WiFi stack audit for taimen (ath10k/WCN3990). |
| `qrtr-lookup.py` | - | `soc:qcom` | Minimal qrtr-lookup: list QMI services on the QRTR bus. No deps.""" |

## Build and packaging

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-build-lock.sh` | - | `generic` | Serialise KERNEL BUILDS across parallel agents. |
| `ph-build.sh` | - | `generic` | shellcheck shell=bash |
| `ph-modcrc.py` | BOOTED | `generic` | Compare the __versions (modversions) sections of two .ko files. |
| `ph-pkgcheck.sh` | BOOTED | `soc:msm8998` | ph-pkgcheck.sh -- is the thing that SHIPS the thing you EDITED? |
| `ph-push-module.sh` | BOOTED | `generic` | Install a freshly built kernel module onto the running phone, no reflash. |
| `ph-reconcile.sh` | - | `soc:msm8998` | ph-reconcile.sh -- does the aport series and the linux/ tree contain the same code? |

## Low level

| tool | needs | scope | what it does |
|---|---|---|---|
| `dtb-grep.py` | - | `soc:qcom` | Print nodes matching a name pattern out of a flattened device tree. |
| `fdtdump.py` | - | `generic` | Minimal flattened-devicetree dumper (no dtc on this host). |
| `ph-i2c-probe.py` | - | `generic` | Scan the taimen touch i2c bus (QUP5) for the STM FTM4 at 0x49. |
| `ph-kmsglog.py` | - | `generic` | Follow /dev/kmsg into a file, fsync'd per record, so the kernel log tail |
| `ph-pins.py` | - | `soc:msm8998` | Sample TLMM GPIO input levels on the phone -- runs ON THE DEVICE. |
| `ph-regdump.py` | - | `generic` | Dump an MMIO range via /dev/mem, one mmap'd process. |
| `ph-spi-probe.py` | - | `soc:msm8998` | Poke the taimen touch controller (LG SW49408) over spidev. |
| `pil-squash.py` | - | `soc:msm8998` | Reassemble a split Qualcomm PIL image (foo.mdt + foo.b00..bNN) into one foo.mbn. |

## Other

| tool | needs | scope | what it does |
|---|---|---|---|
| `gen-tools-doc.py` | - | `generic` | Generate docs/TOOLS.md from the tool headers. Run via `make tools-doc`. |
| `mtstall.py` | on-device; /proc | `generic` | mtstall.py SECONDS [COMM] -- is the WebKit main thread computing, or blocked? |
| `ph-applaunch-bench.sh` | on-device | `generic` | ph-applaunch-bench.sh -- cold/warm application launch latency under phosh. |
| `ph-autologin.sh` | BOOTED | `generic` | Enable or disable greetd auto-login, which is the ONLY reliable way to get an |
| `ph-battery-log.sh` *(profile)* | on-device | `device:google-taimen` | Log real battery drain, and the state needed to explain it. |
| `ph-blankwatch.py` | on-device as the session user; grim; a screen that is ON | `generic` | Sample how much of the screen is BLANK while something is happening. |
| `ph-capture-userspace.sh` | BOOTED | `generic` | Capture the device's locally-built package set, so a from-scratch install |
| `ph-capture.sh` | BOOTED | `generic` | ph-capture.sh -- arm every log channel this device has, from the HOST, and |
| `ph-chromium-conf-check.sh` *(profile)* | - | `device:google-taimen` | ph-chromium-conf-check.sh [confdir] -- runs ON THE HOST. |
| `ph-chromium-videoarm.sh` | on-device as the session user; /tmp/sess.sh; chromium; grim; v4l2-ctl; | `generic` | ph-chromium-videoarm.sh LABEL [CHROMIUM_FLAGS...] -- one PLAYBACK arm. |
| `ph-cmp-vendor-regs.py` *(profile)* | - | `device:google-taimen` | Compare a live WCD934X register dump against downstream's init tables. |
| `ph-crosstax.py` | - | `generic` | ph-crosstax.py [SECONDS] -- how much of a cross build runs under qemu. |
| `ph-daily-audit.sh` | on-device | `soc:msm8998` | Daily-driver readiness audit. Run ON THE DEVICE. |
| `ph-decode-killstorm.sh` | on-device | `generic` | ph-decode-killstorm.sh -- SIGKILL a hardware decode mid-flight, N times, |
| `ph-dt2w-probe.py` *(profile)* | on-device | `device:google-taimen` | The device half of ph-dt2w-test.sh: wait for a finger, arm, watch for a tap. |
| `ph-dt2w-test.sh` *(profile)* | BOOTED | `device:google-taimen` | Prove double-tap-to-wake at the CONTROLLER, without a suspend and without |
| `ph-dtc.sh` | - | `soc:msm8998` | ph-dtc.sh [board.dts] -- compile a board DTS on the HOST, before asking for a |
| `ph-dumpdiff.py` | - | `generic` | Diff a stock-Android TKDUMP capture against our camss port's register state. |
| `ph-egl-dmabuf-probe.py` | on-device | `generic` | What dmabuf modifiers does EGL really advertise, per platform? |
| `ph-fp-morning.sh` | BOOTED | `soc:msm8998` | One-command fingerprint capture test: run this, touch the sensor |
| `ph-fp-ta.sh` | on-device | `soc:msm8998` | ph-fp-ta.sh -- what does the TrustZone say about the fingerprint trustlet? |
| `ph-framprobe.py` *(profile)* | on-device, inside the graphical session | `device:google-taimen` | What frame interval does the compositor actually give a client? |
| `ph-greetd-login.py` | on-device as root | `generic` | Log a user into the graphical session through greetd's IPC, from ssh. |
| `ph-irq-floor.py` | BOOTED | `generic` | What is waking the CPU while nobody is using the phone? |
| `ph-lab.py` | BOOTED | `soc:msm8998` | ph-lab.py -- unattended audio experiment harness for taimen. |
| `ph-mempressure.sh` | on-device | `generic` | ph-mempressure.sh -- one line per second of the memory-ceiling vital signs. |
| `ph-modstack.sh` | on-device | `generic` | Reading the module stack, for tkmod's reload path. SOURCED, never run: |
| `ph-mt-doctor.py` | on-device as root | `generic` | Diagnose and clear phantom touch contacts. |
| `ph-nfc-portal-probe.py` | on-device | `generic` | Assert what an app on the NFC portal's fd can and cannot do. |
| `ph-rangehttp.py` | on-device | `generic` | Serve a directory over HTTP with byte-range support, for <video> benches. |
| `ph-scrollarm.sh` | on-device as the session user | `generic` | ph-scrollarm.sh LABEL [ENV...] -- one PURE-SCROLL arm, with no video playing. |
| `ph-session-cpu.sh` | on-device | `generic` | ph-session-cpu.sh -- rank a graphical session's user units by cumulative CPU. |
| `ph-session.sh` *(profile)* | BOOTED; ph-key.py and ph-touch.py in /tmp on the device; magick on the host | `device:google-taimen` | Wake, unlock and blank the phosh session, reliably enough to drive the phone |
| `ph-skin-burn.sh` *(profile)* | on-device | `device:google-taimen` | Drive skin temperature up under CPU load and record which rungs of the |
| `ph-slpi-dt2w-probe.py` *(profile)* | on-device as root | `device:google-taimen` | Does the SLPI report a double tap once it owns the touch i2c? |
| `ph-stallcatch.py` | on-device as root | `generic` | ph-stallcatch.py SECONDS COMM THREAD_SUBSTR [IDLE_MS] -- when THREAD of the |
| `ph-storage-bench.sh` | on-device | `soc:qcom` | UFS / block / swap measurement for taimen. Run ON THE DEVICE as root: |
| `ph-stream.sh` | BOOTED | `generic` | Stream a command's output from the phone to a host file, across reboots. |
| `ph-strip-btf.py` | - | `generic` | Make a kernel module's .BTF section invisible to the module loader. |
| `ph-threadcpus.py` | on-device; a running process to watch | `generic` | Which CPU does each of a process's threads actually run on, and at what clock. |
| `ph-uclamp.py` | on-device; CONFIG_UCLAMP_TASK=y; same uid as the target | `generic` | Raise the scheduler's utilization floor for a running app's threads. |
| `ph-ui.py` | on-device | `generic` | Drive and inspect the phosh session deterministically. Run ON THE DEVICE. |
| `ph-vibrate.py` | - | `generic` | Play a rumble effect through the force-feedback API. Run ON THE DEVICE. |
| `ph-videoarm.sh` | on-device as the session user | `generic` | ph-videoarm.sh LABEL [ENV...] -- one PLAYBACK arm on a LOCAL clip. |
| `ph-voicecall.py` *(profile)* | BOOTED | `device:google-taimen` | Drive an ADSP voice-call session over the q6voice debugfs bridge. |
| `ph-voicehold.py` *(profile)* | BOOTED | `device:google-taimen` | Hold the voice PCM open for the duration of a call. |
| `ph-webarm.sh` | on-device as the session user | `generic` | ph-webarm.sh LABEL [ENV...] -- one YouTube arm with the page state PROVEN |
| `ph-webbench.sh` | on-device as the session user | `generic` | ph-webbench.sh -- browser smoothness A/B on a deterministic heavy page. |
| `ph-webdraws.sh` | on-device with passwordless sudo; perf; CONFIG_UPROBE_EVENTS; a running | `generic` | ph-webdraws.sh [SECONDS] -- the compositor's draw census per frame. |
| `ph-webeval.py` | on-device; ph-webvq.py beside it; browser launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Evaluate JavaScript in the current page through WebKit's remote inspector. |
| `ph-webframe.sh` | on-device with passwordless sudo; perf; CONFIG_UPROBE_EVENTS; | `generic` | ph-webframe.sh [SECONDS] -- where each compositor frame's period goes. |
| `ph-weblayers.py` | on-device; ph-webvq.py beside it; browser launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Dump the composited layer tree of the current page through the remote inspector. |
| `ph-webvq.py` | on-device; Epiphany launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Poll a <video>'s getVideoPlaybackQuality() through WebKit's remote inspector. |
| `ph-wkoffsets.sh` | BOOTED -- to name the build under test; falls back to the newest apk, | `generic` | ph-wkoffsets.sh [SYMBOL...] -- uprobe offsets for WebKit phase entry points, |
| `ph-wkphase.sh` | on-device with passwordless sudo; CONFIG_UPROBE_EVENTS. Offsets come | `generic` | ph-wkphase.sh arm\|measure [SECONDS]\|off -- where the WebKit MAIN THREAD's |
| `ph-wol.py` | any | `generic` | Wake a sleeping phone over wifi with a WoWLAN magic packet, without hands. |
| `powerhintd.py` | on-device | `generic` | powerhintd -- Android's power hints for a mainline phone. |
| `rootfs-uuid.py` | - | `generic` | Read the filesystem UUIDs out of a pmOS rootfs image, without mounting it. |
| `threadcpu.py` | on-device; /proc | `generic` | threadcpu.py [SECONDS] [PROC-MATCH] -- per-thread CPU of a multithreaded app. |
| `tsh.py` | INITRAMFS | `generic` | Run a command on the pmOS initramfs debug shell (busybox telnetd, $HOST:23). |
| `wkbuckets.py` | - | `generic` | wkbuckets.py PERF-REPORT-FILE -- what phase of WebKit owns the main thread, |

