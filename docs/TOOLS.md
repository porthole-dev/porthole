# Tool catalogue

Generated by `make tools-doc` from each tool's own header. Do not edit.

`porthole tools` is the searchable version of this; `porthole tools --json` is the machine-readable one.

**needs** is the device state a tool requires: `-` host-only, `BOOTED`, `FASTBOOT`, `on-device` (it runs *on* the device), or `any` (it probes and handles several).

146 tools.

## Boot, flash and recovery

| tool | needs | scope | what it does |
|---|---|---|---|
| `boot-probe.sh` | FASTBOOT | `soc:msm8998` | Boot an image and time the USB transitions, distinguishing a real reboot from noise. |
| `bootimg-cmdline.py` | - | `generic` | Read/patch the kernel cmdline of an Android boot image, in place or to a copy. |
| `bootimg-repack-dtb.py` | - | `generic` | Swap the appended DTB inside an Android boot image, fixing up the header. |
| `bootimg-verify.py` | - | `generic` | Verify a boot.img actually carries the kernel/DTB you just built. |
| `dtbo-split.py` | - | `generic` | Split an Android DTBO image into its individual .dtb entries. |
| `tk-flash-boot.sh` | FASTBOOT | `generic` | Flash a boot image to slot b and come back up in pmOS, end to end, unattended. |
| `tk-reboot.sh` | any | `generic` | Reboot the phone and return the INSTANT ssh answers again. |
| `tk-recover.sh` | any | `generic` | tk-recover.sh -- one command to get taimen back after the 2026-08-01 incident, |
| `tk-to-fastboot.sh` | any | `soc:qcom` | Get the phone INTO the bootloader, and return the instant it lands (~9s). |

## Device state and supervision

| tool | needs | scope | what it does |
|---|---|---|---|
| `stallwatch.sh` | BOOTED | `generic` | stallwatch.sh -- detect the taimen PID-1 freeze from the HOST. |
| `tk-audio-cycle.sh` | BOOTED | `soc:msm8998` | tk-audio-cycle.sh [--dtb] [--kernel] -- one command from "make finished" to |
| `tk-cycle.sh` | any | `generic` | One display bring-up experiment: bootloader -> RAM boot -> modprobe -> capture. |
| `tk-deadman.sh` | BOOTED | `soc:msm8998` | tk-deadman.sh -- arm/disarm a self-reboot on the PHONE before a risky test. |
| `tk-device-test.sh` | BOOTED | `generic` | One check: two concurrent tk-device.sh callers must not interleave. |
| `tk-device.sh` | any | `generic` | Serialise access to the ONE physical phone across parallel agents. |
| `tk-hang-matrix.sh` | BOOTED | `soc:msm8998` | tk-hang-matrix.sh -- the 2x2 attribution matrix for defect #3. |
| `tk-lifeline` | on-device | `soc:qcom` | tk-lifeline: if this boot cannot reach the USB host, return to the bootloader. |
| `tk-soak.sh` | on-device | `generic` | tk-soak.sh -- the on-device half of the D2 / 72 h stability soak. |
| `tk-supervise.sh` | any | `generic` | tk-supervise.sh -- keep the phone alive during unattended work. |
| `tk-suspend-cycle.sh` | BOOTED | `soc:qcom` | One real s2idle cycle, driven from the host, with evidence that survives the |
| `tk-wake-cycle.py` | on-device | `generic` | Press the power key on a loop and count the blank/unblank transitions that |
| `tk-wifi-soak.sh` | on-device | `generic` | tk-wifi-soak.sh -- watch a WiFi link for the two ways it fails quietly. |

## Power, suspend and thermal

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-afk.sh` | BOOTED | `generic` | tk-afk.sh -- stop the phone suspending itself while nobody is at the keyboard. |
| `tk-cpufreq-verify.sh` | - | `soc:msm8998` | tk-cpufreq-verify.sh -- does a requested CPU frequency actually get delivered? |
| `tk-osm-probe.py` | - | `soc:msm8998` | Read what the msm8998 OSM actually delivered. |
| `tk-suspend-guard-check.sh` | BOOTED | `generic` | tk-suspend-guard-check.sh -- prove the suspend guard actually blocks, WITHOUT |
| `tk-sysstate.sh` | on-device | `generic` | Print one compact line of system state per second, forever. |
| `tk-thermal-ramp.sh` | - | `generic` | tk-thermal-ramp.sh -- the six-phase thermal ramp, with an abort that works. |
| `tk-thermal.sh` | BOOTED | `generic` | Keep a phone from being cooked by back-to-back measurement arms. |
| `tk-usb-wake.py` | any | `soc:qcom` | Wake a taimen that is wedged in suspend, from the host, without hands. |

## Display and GPU

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-display-watch.py` | - | `soc:msm8998` | Log the display pipeline so a frozen screen can be read off afterwards. |
| `tk-dpu-regs.sh` | BOOTED | `generic` | Dump and decode the DPU registers that matter for a stuck display. |
| `tk-firstpaint.sh` | - | `generic` | tk-firstpaint.sh -- what the user actually waits for: launch -> first frame. |
| `tk-fps.py` | - | `soc:msm8998` | Sample the display/GPU pipeline once a second. Run ON THE DEVICE. |
| `tk-gesture-bench.py` | - | `generic` | Measure how smooth the phosh session actually is. Run ON THE DEVICE as root. |
| `tk-perf-ab.sh` | - | `generic` | tk-perf-ab.sh -- interleaved A/B of a tuning knob against real frame timings. |
| `tk-ringdump.py` | - | `generic` | Decode the ringbuffer out of an adreno devcoredump into CP packets. |

## Camera

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-allphy.py` | - | `soc:msm8998` | Hand-configure ALL THREE msm8998 CSIPHYs and watch every status register. |
| `tk-awb-invert.py` *(profile)* | - | `device:google-taimen` | Invert the SoftwareIsp pipeline over a crop to recover raw (black-subtracted, |
| `tk-camstream.sh` | - | `generic` | tk-camstream.sh -- one capture attempt on the IMX179 front camera, with the |
| `tk-chromatix-awb-locus.py` *(profile)* | - | `device:google-taimen` | Extract the AWB colour-temperature locus from a vendor chromatix 3a blob. |
| `tk-easel-iatu.py` *(profile)* | - | `device:google-taimen` | Program Easel's inbound iATU so BAR2 maps its peripheral window, then read |
| `tk-easel-poke.py` *(profile)* | - | `device:google-taimen` | Poke Easel registers live through BAR2 (HANDOFF §42b). |
| `tk-easel-poll.py` *(profile)* | - | `device:google-taimen` | Poll Easel RX PHY_STOPSTATE / PHY_RX fast, to catch HS data bursts. |
| `tk-easel-pon.py` *(profile)* | - | `device:google-taimen` | Power on Easel's BCM15602 PMIC by hand and see if it answers on i2c. |
| `tk-easel-power.py` *(profile)* | - | `device:google-taimen` | Bring Easel (Pixel Visual Core) all the way up from userspace, then retrain |
| `tk-easel-rx.py` *(profile)* | - | `device:google-taimen` | Read Easel's MIPI RX/TOP state through BAR2 -- a third, independent |
| `tk-easel-txpoll.py` *(profile)* | - | `device:google-taimen` | Poll Easel TX PHY_STATUS to see whether the bypass forwards data bursts.""" |
| `tk-easel-vpg.py` *(profile)* | - | `device:google-taimen` | Drive Easel's TX video pattern generator -- sensor and bypass mux out of the |
| `tk-phy-reinit.py` | - | `generic` | Re-run the CSIPHY v5.0.1 init sequence by hand, WHILE the sensor is already |
| `tk-phy-settle-sweep.py` | - | `generic` | Sweep the CSIPHY settle count over its whole 0..255 range against a LIVE |
| `tk-phy-sweep.py` | - | `generic` | Sweep CSIPHY CMN_CTRL5 (the lane-enable mask) against a LIVE transmitting |
| `tk-phystat.py` | - | `generic` | Read (and optionally poke) the SoC CSIPHY common block live. |
| `tk-sensorstream.sh` | - | `generic` | tk-sensorstream.sh -- stream the REAL sensor path, imx179 -> CSIPHY -> CSID -> |
| `tk-tgstream.sh` | - | `generic` | tk-tgstream.sh -- capture from the CSID's own test generator. |
| `tk-vfe-wrote.py` *(profile)* | - | `device:google-taimen` | Does the VFE write ANY byte to the capture buffer? |

## Audio

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-acoustic.py` | BOOTED | `generic` | tk-acoustic -- decide "does audio actually come out / go in" with no human. |
| `tk-alsa-stage.py` | BOOTED | `generic` | Walk the ALSA PCM lifecycle one call at a time, marking /dev/kmsg before each. |
| `tk-dmic-sweep.sh` *(profile)* | on-device | `device:google-taimen` | tk-dmic-sweep.sh [seconds] -- runs ON THE PHONE. |
| `tk-gap.py` | BOOTED | `generic` | Measure the capture gating (HANDOFF-audio.md 2.5) as a number, per config. |
| `tk-mic-ab.sh` *(profile)* | BOOTED | `device:google-taimen` | tk-mic-ab.sh [seconds] -- runs ON THE PHONE. |
| `tk-mic-check.sh` | BOOTED | `generic` | tk-mic-check.sh -- is the microphone producing AUDIO, or just a noise floor? |
| `tk-mic-clean.sh` *(profile)* | BOOTED | `device:google-taimen` | tk-mic-clean.sh [mic] [seconds] [stray] -- runs ON THE PHONE. |
| `tk-mic-split.sh` *(profile)* | BOOTED | `device:google-taimen` | tk-mic-split.sh -- runs ON THE PHONE. Three captures, one question each. |
| `tk-mic-test.sh` | BOOTED | `generic` | tk-mic-test.sh [DMIC0..DMIC5] [seconds] [rate] -- runs ON THE PHONE. |
| `tk-mic-watch.sh` | BOOTED | `generic` | tk-mic-watch.sh [seconds] -- runs ON THE PHONE. |
| `tk-micwatch.sh` | BOOTED | `generic` | tk-micwatch.sh -- sample whether anything is holding the microphone open. |
| `tk-tas-fw.py` *(profile)* | - | `device:google-taimen` | Parse a TAS2557 uCDSP firmware image and report what is inside it. |
| `tk-tone.py` | - | `generic` | Generate an acoustic probe signal, to be played on the HOST while the phone |
| `tk-wav-verdict.py` | - | `generic` | Decide whether a capture contains real microphone audio. |

## Touch and input

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-evtest.py` | - | `generic` | Dump multitouch events from an evdev node. Run ON THE DEVICE, as root. |
| `tk-ftm4-log.py` *(profile)* | - | `device:google-taimen` | Passively log the whole touch chain so a failure can be read off afterwards. |
| `tk-ftm4-poke.py` *(profile)* | - | `device:google-taimen` | Interrogate the FTM4 touch controller behind the back of a bound ftm4 driver. |
| `tk-key.py` | - | `generic` | Inject a key press through /dev/uinput. Run ON THE DEVICE as root. |
| `tk-mtstate.py` | - | `generic` | Print the CURRENT multitouch slot state of an evdev node, without waiting. |
| `tk-touch.py` | - | `generic` | Inject touch gestures through /dev/uinput. Run ON THE DEVICE as root. |

## Sensors

| tool | needs | scope | what it does |
|---|---|---|---|
| `tk-compass-cal.py` | BOOTED | `soc:qcom` | Calibrate the magnetometer's hard-iron offset, and persist it. |
| `tk-compass-check.py` | - | `generic` | Check the compass maths in qcom_smgr.c against floating point. |
| `tk-iio-events.py` | - | `soc:qcom` | Enable an IIO event and print events as they arrive. Runs ON the device. |
| `tk-iio-read.py` | - | `soc:qcom` | Read samples from a buffer-only IIO device (no in_*_raw), e.g. qcom-smgr-*. |
| `tk-mount-cal.py` | BOOTED | `generic` | Measure the accelerometer mount matrix from four guided poses. |
| `tk-prox-check.py` | BOOTED | `generic` | Check that qcom_smgr suppresses the proximity IR crosstalk in the ALS. |
| `tk-sensor-chain.sh` | BOOTED | `soc:qcom` | tk-sensor-chain.sh -- watch the WHOLE ambient-light / proximity chain at once: |
| `tk-sensor-poke.py` | - | `generic` | Live 16-bit-register I2C access to the IMX179, for use WHILE camss holds the |
| `tk-sns-groups.py` | - | `soc:qcom` | Extract the SSC sensor-registry group table from Android's sensors.qcom. |

## Radios

| tool | needs | scope | what it does |
|---|---|---|---|
| `qrtr-lookup.py` | - | `soc:qcom` | Minimal qrtr-lookup: list QMI services on the QRTR bus. No deps.""" |
| `tk-lte-load.py` | - | `generic` | Generate sustained traffic that REALLY goes out over LTE. Run ON THE DEVICE. |
| `tk-wifi-audit.sh` | - | `generic` | tk-wifi-audit.sh -- end-to-end WiFi stack audit for taimen (ath10k/WCN3990). |

## Build and packaging

| tool | needs | scope | what it does |
|---|---|---|---|
| `ph-build.sh` | - | `generic` | shellcheck shell=bash |
| `tk-build.sh` | - | `generic` | Serialise KERNEL BUILDS across parallel agents. |
| `tk-modcrc.py` | BOOTED | `generic` | Compare the __versions (modversions) sections of two .ko files. |
| `tk-pkgcheck.sh` | BOOTED | `soc:msm8998` | tk-pkgcheck.sh -- is the thing that SHIPS the thing you EDITED? |
| `tk-push-module.sh` | BOOTED | `generic` | Install a freshly built kernel module onto the running phone, no reflash. |
| `tk-reconcile.sh` | - | `soc:msm8998` | tk-reconcile.sh -- does the aport series and the linux/ tree contain the same code? |

## Low level

| tool | needs | scope | what it does |
|---|---|---|---|
| `dtb-grep.py` | - | `soc:qcom` | Print nodes matching a name pattern out of a flattened device tree. |
| `fdtdump.py` | - | `generic` | Minimal flattened-devicetree dumper (no dtc on this host). |
| `pil-squash.py` | - | `soc:msm8998` | Reassemble a split Qualcomm PIL image (foo.mdt + foo.b00..bNN) into one foo.mbn. |
| `tk-i2c-probe.py` | - | `generic` | Scan the taimen touch i2c bus (QUP5) for the STM FTM4 at 0x49. |
| `tk-kmsglog.py` | - | `generic` | Follow /dev/kmsg into a file, fsync'd per record, so the kernel log tail |
| `tk-pins.py` | - | `soc:msm8998` | Sample TLMM GPIO input levels on the phone -- runs ON THE DEVICE. |
| `tk-regdump.py` | - | `generic` | Dump an MMIO range via /dev/mem, one mmap'd process. |
| `tk-spi-probe.py` | - | `soc:msm8998` | Poke the taimen touch controller (LG SW49408) over spidev. |

## Other

| tool | needs | scope | what it does |
|---|---|---|---|
| `gen-tools-doc.py` | - | `generic` | Generate docs/TOOLS.md from the tool headers. Run via `make tools-doc`. |
| `mtstall.py` | on-device; /proc | `generic` | mtstall.py SECONDS [COMM] -- is the WebKit main thread computing, or blocked? |
| `powerhintd.py` | on-device | `generic` | powerhintd -- Android's power hints for a mainline phone. |
| `rootfs-uuid.py` | - | `generic` | Read the filesystem UUIDs out of a pmOS rootfs image, without mounting it. |
| `threadcpu.py` | on-device; /proc | `generic` | threadcpu.py [SECONDS] [PROC-MATCH] -- per-thread CPU of a multithreaded app. |
| `tk-applaunch-bench.sh` | on-device | `generic` | tk-applaunch-bench.sh -- cold/warm application launch latency under phosh. |
| `tk-autologin.sh` | BOOTED | `generic` | Enable or disable greetd auto-login, which is the ONLY reliable way to get an |
| `tk-capture.sh` | BOOTED | `generic` | tk-capture.sh -- arm every log channel this device has, from the HOST, and |
| `tk-cmp-vendor-regs.py` *(profile)* | - | `device:google-taimen` | Compare a live WCD934X register dump against downstream's init tables. |
| `tk-daily-audit.sh` | on-device | `soc:msm8998` | Daily-driver readiness audit. Run ON THE DEVICE. |
| `tk-decode-killstorm.sh` | on-device | `generic` | tk-decode-killstorm.sh -- SIGKILL a hardware decode mid-flight, N times, |
| `tk-dtc.sh` | - | `soc:msm8998` | tk-dtc.sh [board.dts] -- compile a board DTS on the HOST, before asking for a |
| `tk-dumpdiff.py` | - | `generic` | Diff a stock-Android TKDUMP capture against our camss port's register state. |
| `tk-egl-dmabuf-probe.py` | on-device | `generic` | What dmabuf modifiers does EGL really advertise, per platform? |
| `tk-fp-probe.sh` | - | `soc:msm8998` | tk-fp-probe.sh -- is the fingerprint sensor physically alive? |
| `tk-framprobe.py` *(profile)* | on-device, inside the graphical session | `device:google-taimen` | What frame interval does the compositor actually give a client? |
| `tk-greetd-login.py` | on-device as root | `generic` | Log a user into the graphical session through greetd's IPC, from ssh. |
| `tk-lab.py` | BOOTED | `soc:msm8998` | tk-lab -- unattended audio experiment harness for taimen. |
| `tk-mempressure.sh` | on-device | `generic` | tk-mempressure.sh -- one line per second of the memory-ceiling vital signs. |
| `tk-modstack.sh` | on-device | `generic` | Reading the module stack, for tkmod's reload path. SOURCED, never run: |
| `tk-mt-doctor.py` | on-device as root | `generic` | Diagnose and clear phantom touch contacts. |
| `tk-rangehttp.py` | on-device | `generic` | Serve a directory over HTTP with byte-range support, for <video> benches. |
| `tk-scrollarm.sh` | on-device as the session user | `generic` | tk-scrollarm.sh LABEL [ENV...] -- one PURE-SCROLL arm, with no video playing. |
| `tk-session-cpu.sh` | on-device | `generic` | tk-session-cpu.sh -- rank a graphical session's user units by cumulative CPU. |
| `tk-session.sh` *(profile)* | BOOTED; tk-key.py and tk-touch.py in /tmp on the device; magick on the host | `device:google-taimen` | Wake, unlock and blank the phosh session, reliably enough to drive the phone |
| `tk-storage-bench.sh` | on-device | `soc:qcom` | UFS / block / swap measurement for taimen. Run ON THE DEVICE as root: |
| `tk-stream.sh` | BOOTED | `generic` | Stream a command's output from the phone to a host file, across reboots. |
| `tk-strip-btf.py` | - | `generic` | Make a kernel module's .BTF section invisible to the module loader. |
| `tk-ui.py` | on-device | `generic` | Drive and inspect the phosh session deterministically. Run ON THE DEVICE. |
| `tk-vibrate.py` | - | `generic` | Play a rumble effect through the force-feedback API. Run ON THE DEVICE. |
| `tk-videoarm.sh` | on-device as the session user | `generic` | tk-videoarm.sh LABEL [ENV...] -- one PLAYBACK arm on a LOCAL clip. |
| `tk-voicecall.py` *(profile)* | BOOTED | `device:google-taimen` | Drive an ADSP voice-call session over the q6voice debugfs bridge. |
| `tk-voicehold.py` *(profile)* | BOOTED | `device:google-taimen` | Hold the voice PCM open for the duration of a call. |
| `tk-webarm.sh` | on-device as the session user | `generic` | tk-webarm.sh LABEL [ENV...] -- one YouTube arm with the page state PROVEN |
| `tk-webbench.sh` | on-device as the session user | `generic` | tk-webbench.sh -- browser smoothness A/B on a deterministic heavy page. |
| `tk-webdraws.sh` | on-device with passwordless sudo; perf; CONFIG_UPROBE_EVENTS; a running | `generic` | tk-webdraws.sh [SECONDS] -- the compositor's draw census per frame. |
| `tk-webeval.py` | on-device; tk-webvq.py beside it; browser launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Evaluate JavaScript in the current page through WebKit's remote inspector. |
| `tk-webframe.sh` | on-device with passwordless sudo; perf; CONFIG_UPROBE_EVENTS; | `generic` | tk-webframe.sh [SECONDS] -- where each compositor frame's period goes. |
| `tk-weblayers.py` | on-device; tk-webvq.py beside it; browser launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Dump the composited layer tree of the current page through the remote inspector. |
| `tk-webvq.py` | on-device; Epiphany launched with WEBKIT_INSPECTOR_HTTP_SERVER=127.0.0.1:9222 | `generic` | Poll a <video>'s getVideoPlaybackQuality() through WebKit's remote inspector. |
| `tk-wkoffsets.sh` | HOST side. nm and readelf; the -dbg and the plain webkit apk for the | `generic` | tk-wkoffsets.sh [SYMBOL...] -- uprobe offsets for WebKit phase entry points. |
| `tk-wkphase.sh` | on-device with passwordless sudo; CONFIG_UPROBE_EVENTS. Offsets come | `generic` | tk-wkphase.sh arm\|measure [SECONDS]\|off -- where the WebKit MAIN THREAD's |
| `tsh.py` | INITRAMFS | `generic` | Run a command on the pmOS initramfs debug shell (busybox telnetd, $HOST:23). |
| `wkbuckets.py` | a `perf script -F sym` dump; run anywhere | `generic` | wkbuckets.py PERF-REPORT-FILE -- what phase of WebKit owns the main thread. |

