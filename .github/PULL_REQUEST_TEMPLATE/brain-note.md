## What this note records

<!-- One sentence. If it needs an "and", it is two notes. -->

## Would this have saved someone a session?

<!-- If the honest answer is no, it is a note to yourself rather than one for
     the corpus, and that is fine -- keep it in your own notes. -->

## Evidence

<!-- What proves it, in a form a stranger can re-check: a command and its
     output, a commit, a file and line, a dated observation. A trap without a
     source is folklore, and folklore is what this corpus exists to replace. -->

## Scope

- [ ] `generic` — and I have thought about whether that is honest
- [ ] `soc:<soc>` — it depends on this silicon
- [ ] `device:<codename>` — it only ever applied to this device

Over-claiming portability is worse than scoping narrowly.

## Checklist

- [ ] `porthole brain --lint` passes
- [ ] the symptom is described before the cause (people search by symptom)
- [ ] `porthole brain --reindex` run
- [ ] `Signed-off-by:` on the commit
